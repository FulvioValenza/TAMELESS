package it.polito.dp2.TAMELESS.ass;

import java.util.Set;

import javax.ws.rs.BadRequestException;
import javax.ws.rs.NotFoundException;

import it.polito.dp2.TAMELESS.sol.client.BasicProperty1EntityName;
import it.polito.dp2.TAMELESS.sol.client.EntityNature;


/**
 * An interface for operating on a DerivedProperty1Entity through a remote service
 *
 */
public interface DerivedProperty1Entity extends DerivedProperty {

	/*
	 * At the moment, this interface is unused but created only for mantaining the same structure 
	 * of Property1Entity and Property1Entity1Threat
	 */
}
